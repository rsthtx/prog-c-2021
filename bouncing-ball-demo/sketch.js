let xPos = 40;

function setup() {
  createCanvas(400, 500)
}

function draw() {
  background(220);
  
  circle(xPos, 200, 50)
  
  // xPos = xPos + 1
  xPos  += 1
}

Hold B har dansk i 2-4 lektion i lokale 4005 i stedet for 4101